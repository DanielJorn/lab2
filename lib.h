#include <iostream>

using namespace std;
/* Returns the path to the directory with the data  */
string requestDirectoryPath();

/* Returns the team count which is the first line in the file */
int getTeamCount(const string& path);